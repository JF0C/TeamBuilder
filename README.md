# Team Builder

## Setup
1) local deployment over sftp  
    1) install virtual environment
        ```
        cd deployment
        python3 -m venv .
        source ./bin/activate
        pip3 install -r requirements.txt
        ```